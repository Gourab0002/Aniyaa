# Copy to keystore.properties (gitignored) and fill in local values.
# CI uses KEYSTORE_FILE / KEYSTORE_PASSWORD / KEY_ALIAS / KEY_PASSWORD instead.

storeFile=C:/Users/you/aniyaa-release.keystore
storePassword=
keyAlias=aniyaa
keyPassword=
